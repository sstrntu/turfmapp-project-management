apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "turfmapp.fullname" . }}-test-connection"
  labels:
    {{- include "turfmapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "turfmapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
